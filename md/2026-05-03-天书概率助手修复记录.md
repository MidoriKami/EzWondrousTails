# 2026-05-03 天书概率助手修复记录

## 背景

今天对 `E:\git\EzWondrousTails` 里的 `WondrousTailsSolver` 做了一轮兼容、显示和整理工作，目标是让插件能够在当前 `hooks/dev` 环境下正常编译、正常加载，并把概率信息稳定显示回游戏里的天书界面。

## 今天完成的事情

### 1. 兼容 `hooks/dev` 并修复编译/加载问题

- 适配了当前 Dalamud `hooks/dev` 环境。
- 修复了插件在新环境下的编译问题。
- 修复了插件加载阶段的兼容性问题，确保可以正常构造并进入运行状态。
- 当前验证命令：

```powershell
dotnet build .\WondrousTailsSolver.sln -c Debug
```

- 当前构建结果为：`0 warning / 0 error`

### 2. 修复插件扫描与输出目录问题

- 补全并修正了插件元数据，确保开发插件扫描能够识别插件。
- 按本地导入的方式整理过输出目录结构。
- 本轮清理中已把 `WondrousTailsSolver/output/` 重新视为构建产物，不再作为源码仓库内容保留。
- 同时在根目录 `.gitignore` 里增加了：

```text
/WondrousTailsSolver/output/
```

### 3. 中文化与名称调整

- 插件可见名称已调整为：`天书概率助手`
- 主窗口和设置窗口已改成中文显示。
- `Punchline` 和 `Description` 已整理为中文在前、英文在后的双语形式。
- “二次机会点数” 已改成 `[胡思乱想]点数`

### 4. 主窗口显示天书信息

- 保留了主窗口显示能力，主窗口可以查看：
- 已贴贴纸数量
- `[胡思乱想]` 点数
- `1线 / 2线 / 3线` 概率
- `重排平均`
- 当前 4x4 棋盘状态

### 5. 把概率信息放回游戏界面

- 早期尝试过通过悬浮覆盖层显示概率。
- 最终方案改为直接改写天书界面底部说明文字区域，不再额外悬浮显示。
- 当前底部说明区会按三行显示：
- 原说明文本
- 空白行
- `连线概率：...`
- `重排平均：...`

### 6. 文本细节调整

- 将“空白处贴上印花”后的测试替换逻辑收口到当前正式实现。
- 在“贴上印花”下面增加了一行空白行，让概率信息整体下移，避免界面挤在一起。
- 说明文本节点会自动切换为多行并按需要增高，插件卸载时会恢复原文字、高度和文本标志。

### 7. 精简依赖并瘦身

- 移除了 `WondrousTailsSolver` 对 `KamiToolKit` 的项目依赖。
- 将原本依赖 `KamiToolKit.AddonController` 的界面监听逻辑，改为直接使用 Dalamud 原生 `IAddonLifecycle` 和 `IGameGui`。
- 新增了轻量服务入口文件：
  - `WondrousTailsSolver/DalamudServices.cs`
- 解决方案中不再包含 `KamiToolKit` 项目。
- 仓库层面的 `KamiToolKit` 子模块也已彻底移除。
- 精简后，插件输出目录只保留：
  - `WondrousTailsSolver.dll`
  - `WondrousTailsSolver.deps.json`
  - `WondrousTailsSolver.json`
- `KamiToolKit.dll` 和 `SixLabors.ImageSharp.dll` 不再进入插件输出，体积明显下降。

## 今天涉及的主要文件

- `WondrousTailsSolver/AddonWeeklyBingoController.cs`
- `WondrousTailsSolver/PerfectTails.cs`
- `WondrousTailsSolver/WondrousTailsSolverPlugin.cs`
- `WondrousTailsSolver/DalamudServices.cs`
- `WondrousTailsSolver/Ui/MainWindow.cs`
- `WondrousTailsSolver/Ui/ConfigWindow.cs`
- `WondrousTailsSolver/WondrousTailsSolver.csproj`
- `WondrousTailsSolver/WondrousTailsSolver.yaml`
- `WondrousTailsSolver.sln`
- `.gitignore`

## 这次顺手做的清理

- 删除了仓库中的本地构建输出目录 `WondrousTailsSolver/output/`
- 去掉了已经不再使用的悬浮覆盖层空入口
- 重写了原先乱码的工作记录文件
- 将插件项目从 `KamiToolKit` 依赖中剥离，减少不必要输出
- 删除了 `.gitmodules` 和 `KamiToolKit` 子模块目录

## 当前结论

项目主体已经整理到“保留功能代码、移除构建产物、文档可读”的状态。
如果下一步要继续收尾，优先建议：

1. 根据游戏内最终截图再微调底部三行文字的位置与高度。
2. 再做一次完整提交前的工作区检查。
